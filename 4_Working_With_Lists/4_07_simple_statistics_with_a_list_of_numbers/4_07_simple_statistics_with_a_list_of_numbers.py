# We can easily find the min(), max(), sum() of the numerical values inside a list.

numbers = list(range(1, 101, 7))
print(numbers)

# MAX function
print(max(numbers))

# MIN function
print(min(numbers))

# SUM function
print(sum(numbers))